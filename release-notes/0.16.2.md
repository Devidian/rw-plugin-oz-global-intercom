## Translation fixes

- Fix raw translation keys appearing in welcome, channel, chat and status messages.
- Restore translated relay responses and add missing reconnect and initialization messages.
- Correct Russian on/off translations.

Install the updated plugin together with its bundled `i18n/*.json` files. Existing relay configuration remains compatible.
